# Next.js frontend environment variables.
# Copy to .env.local for local dev; set in Cloudflare Pages UI for production.

# Base URL of the Flask API backend (no trailing slash).
# Local dev:   http://localhost:5000
# Production:  https://api.axiompicks.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
